@model IEnumerable<WAD_Final_12435.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capacity)
            </th>
            <th>
                @Html.DisplayNameFor((Category model) => model.CategoryTypeEnum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Capacity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoryTypeEnum)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedAt)
                </td>
                <td>
                    <button type="button" class="btn btn-light">
                        @Html.ActionLink("Edit", "Edit", new {id=item.ID})</button>
                    <button type="button" class="btn btn-primary">
                        @Html.ActionLink("Details", "Details", new {id=item.ID})</button>
                    <button type="button" class="btn btn-warning">@Html.ActionLink("Delete", "Delete", new {id=item.ID})</button>
                </td>
            </tr>
        }
    </tbody>
</table>
